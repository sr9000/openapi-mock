apiVersion: 1

providers:
  - name: 'gRPC Mock Dashboards'
    orgId: 1
    folder: 'gRPC Mock'
    folderUid: 'openapi-mock'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
